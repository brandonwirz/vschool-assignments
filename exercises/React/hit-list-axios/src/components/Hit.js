import React from "react";


function Hit(props){
        return(
          <div>
                <div className="last">{props.name}</div>
                {/* <img className="images" src={props.src} alt={props.name}/> */}
          </div>
    );
}


export default Hit;
